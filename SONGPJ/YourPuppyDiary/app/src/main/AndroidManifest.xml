<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.your_puppy_diary">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:name=".main.di.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_dog_round"
        android:label="@string/app_name_japan"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_dog_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="q">

        <activity
            android:name=".main.ui.splash.SplashScreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainTopActivity" />

        <receiver
            android:name=".main.AlarmReceiver"
            android:exported="false" />

        <activity
            android:name=".main.calendarMemo.CalendarMemoActivity"
            android:theme="@style/Transparent" />
    </application>
</manifest>